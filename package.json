{
  "name": "kenpo-watcher",
  "version": "1.0.0",
  "scripts": {
    "bundle": "esbuild index.js --bundle --platform=node --outdir=dist",
    "build-binary": "pkg dist/index.js --targets node16-linux-x64 --output kenpo-watcher"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "puppeteer": "^21.3.8"
  },
  "devDependencies": {
    "pkg": "^5.8.0",
    "esbuild": "^0.19.2"
  }
}
